{
  "name": "OpenCode Dev Container",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-22-bookworm",
  "postCreateCommand": "curl -fsSL https://bun.sh/install | bash -s -- bun-v$(node -p \"require('./package.json').packageManager.split('@')[1]\") && curl -fsSL https://qlty.sh | QLTY_VERSION=0.643.0 QLTY_INSTALL_BIN_PATH=/home/node/.local/bin QLTY_NO_MODIFY_PATH=1 sh && curl -fsSL https://claude.ai/install.sh | bash && curl -fsSL https://chatgpt.com/codex/install.sh | sh",
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/home/node/.local/bin:/home/node/.bun/bin"
  },
  "mounts": [
    "source=claude-config,target=/home/node/.claude,type=volume",
    "source=codex-config,target=/home/node/.codex,type=volume"
  ]
}
